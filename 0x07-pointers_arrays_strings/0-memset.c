#include "main.h"
/**
 * _memset - fill ablock of memory with a value
 * @s: starting address of memory to be filled
 * @b: the desired value
 * @n: number of bytes to be changed
 * return: chande array with new value for n bytes
 */
char *_memset(char *s, char b, unsigned int n)
{
        int i = 0
                 for (; n > 0; i++)
                 {
                         si[i] =b
                                 n--;
                 }
        return (s)
}
