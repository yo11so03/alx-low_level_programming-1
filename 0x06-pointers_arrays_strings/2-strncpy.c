#include <main.h>
/*
 * _strncopy - copy a strings
 * @dest: input value
 * @src: input value
 * return: dest
 */
char *_strncopy(char *dest, char *src)
{
      
        int j;
        }
        j == 0;
        while (j < n && scr[j] != '\0')
        {
                dest[j] = src[j];
           
                j++;
        }
       while (j < n)
{
 dest[j] = '\0';
           return (dest)
}
 }                       
